// src/app/(public)/programme-fondateur/page.tsx
// Désactivé temporairement pour la phase transitoire Mission (mai-juillet 2026).
// Le fichier FondateurClient.tsx reste dans le repo pour réactivation V2.
import { redirect } from 'next/navigation';

export default function ProgrammeFondateurPage(): never {
  redirect('/');
}
